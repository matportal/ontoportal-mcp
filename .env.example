# OntoPortal MCP server sample configuration
# Copy this file to .env and adjust values for your environment.

# OntoPortal REST API base URL (can also be overridden per connection via MCP URL query: base_url=...)
ONTO_PORTAL_BASE_URL=http://rest.matportal.org

# OntoPortal API key (replace with your own; clients can override per connection using api_key=...)
ONTO_PORTAL_API_KEY=changeme

# MCP server listening address
MCP_HOST=0.0.0.0
MCP_PORT=8000

# Optional bearer token passed to OntoPortalMCPClient when needed
# MCP_BEARER_TOKEN=
